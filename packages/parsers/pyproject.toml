[project]
name = "ka-parsers"
version = "0.1.0"
description = "DocumentParser interface + Markdown/Text"
requires-python = ">=3.11"
dependencies = [
    "ka-common",
]

[tool.uv.sources]
ka-common = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
