[project]
name = "ka-business-mcp"
version = "0.1.0"
description = "Business MCP Server"
requires-python = ">=3.11"
dependencies = [
    "ka-common",
]

[tool.uv.sources]
ka-common = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
