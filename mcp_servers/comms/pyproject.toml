[project]
name = "ka-comms-mcp"
version = "0.1.0"
description = "Comms MCP Server placeholder"
requires-python = ">=3.11"
dependencies = [
    "ka-common",
]

[tool.uv.sources]
ka-common = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
